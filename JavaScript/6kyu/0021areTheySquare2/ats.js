let isSquare = function(arr) {
    function innerArray(arr) {
        function prime(input) {

        }
        if(Array.isArray(arr)) {

        }
    }
    for(let i = 0, len = arr.length(); i < len; i++) {
        for (let )
    }
}